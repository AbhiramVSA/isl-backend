[project]
name = "sign-lang"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kryxen", email = "kryxen.dev@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "kaggle>=2.2.4",
    "mediapipe>=1.0.0",
    "numpy>=2.5.2",
    "opencv-python>=5.0.0.93",
    "torch>=2.13.0",
]

[project.scripts]
sign-lang = "sign_lang:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
